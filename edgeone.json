{
  "name": "flare-status",
  "framework": "vite",
  "buildCommand": "pnpm build",
  "outputDirectory": "dist",
  "schedules": [
    {
      "name": "flarestatus-cron-probe",
      "cron": "*/2 * * * *",
      "path": "/api/cron-probe",
      "method": "POST",
      "timezone": "Asia/Shanghai"
    }
  ]
}
